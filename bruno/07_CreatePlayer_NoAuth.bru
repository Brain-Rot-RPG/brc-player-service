meta {
  name: Player Service - Create Player (No Auth)
  type: http
  seq: 7
}

post {
  url: http://localhost:4004/api/v1/player
  body: json
  auth: none
}

headers {
}

body:json {
  {
    "level": 1.0,
    "brainrotId": "1",
    "gold": 100
  }
}

tests {
  test("Status is 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
  
  test("Returns Forbidden message", function() {
    expect(res.getBody().message).to.include('Forbidden');
  });
}
